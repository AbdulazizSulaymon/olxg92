const initialState = {
    data: [
        {
            name: "Salah"
        },
        {
            name: "Oybek"
        },
        {
            name: "Jamshid"
        }
    ]
}

const reducer = (state = initialState, action) => {
    switch (action.type) {
        default: return state;
    }
}

export default reducer;